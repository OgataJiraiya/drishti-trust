# Inference Integrity / Security Module

Owner branch: `feat/security-backend`

Primary scope:

- signed inference receipts
- SHA-256 artifact identity
- Ed25519 signatures
- replay detection
- model substitution verification
- common evidence API
- tamper-evident audit chain
- assurance summary backend
- Finding Schema v1 output using module `inference_integrity`

Keep Person-3 security/provenance work here and in shared backend/API packages where integration requires it.
